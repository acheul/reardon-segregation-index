[package]
name = "segindex-wasm"
description = "wasm-bindgen bindings for the segindex crate (demo page build only, not published)"
publish = false
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
segindex = { path = "../../rust" }
wasm-bindgen = "0.2"

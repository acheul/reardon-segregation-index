{
  "name": "segindex-page",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build ../wasm --target web --out-dir ../app/src/pkg",
    "dev": "vite",
    "build": "pnpm run wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
